const express = require('express'); // Framework
const mogran = require('morgan'); // To access req,res,next
const bodyParser = require('body-parser'); // To parse de request
const sequelize = require('sequelize'); //ORM
